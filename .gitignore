.superpowers/
.DS_Store
.build/
Aura/Resources/MapboxAccessToken

# Xcode (generated by XcodeGen)
Aura/Aura.xcodeproj/
Aura/build/
*.xcconfig.local
xcuserdata/

# Mapbox secret downloads token (per-worktree, never commit)
.mapbox

# Local device/sim build output (xcodebuild -derivedDataPath)
Aura/build-device/
Aura/build-sim/
build-device/
build-sim/
build-device.log
build-sim.log

# Python bytecode (gem authoring tools)
__pycache__/
*.pyc

# Ride data pulled off a device for debugging (raw SwiftData store + decoded
# tracks). Local, disposable, and contains real GPS traces — never commit.
ride-data-export/
